#include "main.h"

/**
 * main - tests function to print in an  integer is positive or negative number
 * Return: 0 on success
 */

int main(void)
{
        int i;

        i = 0;
        posivite_or_negative(i);

        return (0);
}
